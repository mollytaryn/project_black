<header class="Header">
  <div class="Logo Header-logo">project black</div>
  <% if user_signed_in? %>
    <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
  <% end %>
</header>
